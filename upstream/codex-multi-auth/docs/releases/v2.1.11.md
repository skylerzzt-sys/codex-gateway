# v2.1.11

Adds a marketplace icon.

```bash
npm i -g codex-multi-auth
```

A vector icon for the Codex marketplace and composer surfaces, wired up through `interface.composerIcon` in the plugin manifest so marketplace scanners and curated plugin lists can display it.

No functional changes.
