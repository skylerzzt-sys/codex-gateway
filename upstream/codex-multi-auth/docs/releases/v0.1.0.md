# v0.1.0

The first stable release.

```bash
npm i -g codex-multi-auth
```

Multi-account OAuth for Codex: sign in to several accounts, manage them from one place, and switch between them.

- The `codex auth ...` command family for account lifecycle management
- A dashboard with settings and runtime reliability controls
